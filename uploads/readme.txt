To add new images or videos:
1. Copy your file into this `uploads` folder (or the `images` folder).
2. Open `projects.json` in the parent directory.
3. Edit the `image_url` field of your project to point to your file.
   Example: "image_url": "/uploads/my-video.mp4"
